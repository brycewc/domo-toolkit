---
published: false
---

# To-Do List

- [ ] What views have titles without subtext?
- [ ] How many views use shareAll action? Should that move to a custom action instead of being owned by DataList?
- [ ] I am bad at committing often and with better commit messages. Write a skill to have Claude prompt me to commit and auto-generate messages.
- [ ] Auto map doesn't have visual feedback
- [ ] Beast modes overall checkbox should be disabled if all children are disabled
- [ ] Reload button for migrate content view
- [ ] Exclude /docs from vite hot reload
- [ ] Final confirm modal for migrate content has the two datasets right next to each other in a sentence and it is not easy to read
- [ ] Adjust activity log object type chips to match context footer (icon and hover for key).
- [ ] Should sidepanel views be tab specific?
- [ ] Implement a check for page access, so we populate the page name from that endpoint instead if they don't have access (/api/content/v1/pages/{id}/access) and can turn off share if they do (or maybe change it to a remove)