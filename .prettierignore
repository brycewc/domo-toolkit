# Build output and generated artifacts (mirrors .gitignore so `prettier .` is safe)
dist/
dist-ssr/
release/
.yarn/
.visuals/
.heroui-docs/

# YAML is left to its own tooling (Yarn, Jekyll); not Prettier-formatted.
*.yml
*.yaml

# Jekyll docs site: generated output (_site) plus the vendored minima theme.
# These are regenerated/third-party, not hand-edited source.
docs/_site/
docs/_sass/
docs/_includes/
docs/_layouts/
docs/assets/

# Machine-local Claude settings (untracked).
.claude/settings.local.json

# Vendored skill definitions, left as-is to avoid churn when skills update.
.agents/

# Personal scratch list, left unformatted.
docs/TODO.md
